NEXT_PUBLIC_API_URL=http://localhost:4000
# Google Workspace SSO client id (admin). Empty locally: the dev sign-in is used instead.
NEXT_PUBLIC_ADMIN_GOOGLE_CLIENT_ID=
# true only locally/CI: shows the development sign-in (the API also refuses it in production).
NEXT_PUBLIC_ADMIN_DEV_LOGIN=true
